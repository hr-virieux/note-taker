{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "A simple note-taking application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "predeploy": "cp -a public/. ./",
    "deploy": "git add . && git commit -m 'Deploy to GitHub Pages' && git push origin main"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "uuid": "^3.4.0"
  }
}
